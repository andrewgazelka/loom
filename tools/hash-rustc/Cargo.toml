[package]
name = "hash-rustc"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
blake3 = "1.8"
petgraph = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[package.metadata.rust-analyzer]
rustc_private = true
